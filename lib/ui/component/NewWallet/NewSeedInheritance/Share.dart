import 'package:flutter/material.dart';

class Share2Details extends StatelessWidget {
  const Share2Details({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
