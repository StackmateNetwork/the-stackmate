name: libcpclient
description: cpclient (rust) ffi
version: 0.1.0

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  ffi: ^2.0.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.7.2

flutter:
  plugin:
    platforms:
      android:
        package: com.example.libcpclient
        pluginClass: LibCPClientPlugin
      ios:
        pluginClass: LibCPClientPlugin
